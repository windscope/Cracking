#include <iostream>
#include <vector>
#include <algorithm>

using namespace std;

class QuickSort {
private:
    void partition(vector<int>& data, int pivot_index) {
        int pivot = data[pivot_index];

    }

    void divde(vector<int>& data) {

    }

public:
    void sort(vector<int>& data) {

    }
};
